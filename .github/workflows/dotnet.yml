# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs: 
  build:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        version: ["8.0", "9.0", "10.0"]

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ matrix.version }}.x
    - name: Build CFS.SnabNet.Tests
      run: dotnet build ./CFS.SnabNet.Tests/CFS.SnabNet.Tests.csproj --framework "net${{ matrix.version }}"
    - name: Run CFS.SnabNet.Tests
      run: dotnet test --no-build --verbosity normal --framework "net${{ matrix.version }}" 
